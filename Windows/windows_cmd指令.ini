win r 输入cmd 打开命令符

进入根目录
cd \

进入上级指令
cd ..

切换D盘
在c:根目录 输入 d:

建立文件夹
mkdir [name]

建立空文件
cd >a.txt

建立非空文件 覆盖原来文件
echo a>a.txt

追加文本
echo b>>a.txt

查看文件夹下子文件
dir /b

删除文件夹和里面所有内容
rmdir -f [pkg_name]

删除文件
del

强制删除文件
del -f [file_name]
